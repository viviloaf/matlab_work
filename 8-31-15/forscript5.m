b=[1 10 11 1 1.5 6]
for a_number= b
    if(a_number<5)
        b_number=a_number+1
    else
        break %it tries only the first value, and if it is not fufiklled, 
        %then the script stops. ex; the first value is 1, aso it can add 2,
        %however the next value is 10, and the else command tells the
        %script to break, and it stops the loop in the script.
    end
end
